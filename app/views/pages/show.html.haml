= link_to(project_pages_path , class: 'btn', rel: 'tooltip', title: t('tooltips.back')) do
    %i.icon-arrow-left

= link_to(edit_project_pages_path(@project), class: 'btn', rel: 'tooltip', title: t('tooltips.edit')) do
    %i.icon-pencil

%p
%b Name:
= @page.name
%p
%b Content:
= @page.content
