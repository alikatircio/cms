= simple_form_for(@project, :html => { :class => 'form-inline validate' }) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description

  .form-actions
    = f.button :submit