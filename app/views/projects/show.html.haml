= link_to(projects_path , class: 'btn', rel: 'tooltip', title: t('tooltips.back')) do
    %i.icon-arrow-left

= link_to(edit_project_path(@project), class: 'btn', rel: 'tooltip', title: t('tooltips.edit')) do
    %i.icon-pencil

%p
%b Name:
= @project.name
%p
%b Description:
= @project.description
